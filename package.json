{
  "name": "root",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap --ignore-scripts",
    "dev": "lerna run --scope @eyevinn/web-player-demo start --stream"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "prettier": "^2.1.1",
    "sass": "^1.26.10",
    "typescript": "^4.0.2"
  }
}
